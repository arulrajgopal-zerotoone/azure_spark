name: Build and deploy


on:
  push:
    branches:
      - main
    paths:
      - 'spark_single_vm/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print start successful
        run: echo "start successful"